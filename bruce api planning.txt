Base largely off of elGoose API

elGoose methods:
	setlists
	latest setlist
	shows
	songs
	venues
	jamcharts (not needed)
	albums
	setlist metadata?
	links (attached to shows)
	uploads (show metadata including images)
	appearances (musician appearances)

Returns data in JSON and HTML formats.